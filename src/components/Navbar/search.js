import React from "react";
import { css } from '@emotion/react';
const Search = () => {

    return (
        <div css={op}>
            search
        </div>
    )
}

export const op = css`
padding: 10px; 
background-color: red; 
`;

export default Search;